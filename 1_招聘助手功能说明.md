# 智聘·双向精准匹配引擎 — 完整产品功能描述

---

## 一、产品概述

一款通过"题库测评 + 评分引擎计分"实现**人才画像标准化**、**去标签化双向匹配**、**动态信誉评价**，并为**求职者提供可视化成长追踪与成长目标规划**的智能招聘平台。本产品为纯前端 Web 应用，通过 Supabase SDK 直接实现数据持久化，无需独立后端服务。

**Demo 阶段聚焦岗位**：AI工程师、3D建模师。

---

## 二、技术架构

| 层级 | 技术选型 | 说明 |
|:---|:---|:---|
| **前端框架** | React 18 + TypeScript + Vite 5 | 构建用户界面，支持求职者 / 大学生 / HR 三种模式切换 |
| **状态管理** | Zustand | 全局状态管理，支持乐观更新与失败回滚策略 |
| **数据库** | Supabase (PostgreSQL) | 存储用户画像、岗位数据、成长记录、答题结果、信誉评价等 |
| **AI 服务** | DeepSeek API (`deepseek-v4-flash`) | 画像报告生成、成长规划、学习资源推荐、项目实践方向建议 |
| **测试** | Vitest | 65 项单元测试覆盖数据转换、错误处理、Store 逻辑、同步策略 |
| **部署** | IGA Pages (前端) + Supabase (数据库) | 生成公网可访问链接 |

### 数据持久化架构

```
用户操作 ──► Zustand Store (离线优先，即时响应)
                  │
                  ├──► localStorage (Zustand persist，刷新不丢失)
                  │
                  ▼
          SyncProvider (useSupabaseSync Hook)
                  │
                  ▼
          database.ts (CRUD + 超时保护)
                  │
                  ▼
          supabaseClient.ts (@supabase/supabase-js)
                  │
                  ▼
          Supabase PostgreSQL (7 张数据表)
```

- **离线优先策略**：用户操作先写入本地 Zustand Store + localStorage 双重持久化，UI 即时响应。Supabase 远程同步为 best-effort 模式，网络不通或超时时本地数据不丢失，静默降级
- **Zustand persist 中间件**：关键状态（用户模式、岗位选择、画像历史、HR岗位、求职者岗位、成长记录、候选人）通过 `localStorage` 持久化，页面刷新后数据完整保留
- **同步状态指示器**：全局顶部横幅显示连接状态（蓝色加载中 / 金色同步中 / 红色异常），让用户随时了解数据同步情况
- **本地 ID 机制**：新建数据使用 `hrjob_`、`jsjob_`、`job_`、`hrc_` 前缀作为临时 ID，上传 Supabase 时自动去除，由数据库生成真实 UUID

### 安全设计

| 安全机制 | 说明 |
|:---|:---|
| **Supabase RLS** | 数据库行级安全策略控制数据访问权限 |
| **匿名认证** | `ensureAnonymousSession()` 自动创建匿名用户，实现无感数据同步 |
| **环境变量** | Supabase Project URL 和 Anon Key 通过 `.env` 文件配置，不硬编码在源码中 |
| **会话持久化** | `persistSession: true` + `autoRefreshToken: true`，页面刷新后保持登录状态 |
| **API Key 用户自配置** | DeepSeek API Key 由用户在前端界面输入，不存储在代码或环境中，前端提供输入框和校验 |

---

## 三、用户模式与功能矩阵

### 3.1 三种用户模式

网页支持通过顶部标签栏在以下三种模式之间切换，切换时自动重置当前问答进度和选中的岗位：

| 模式 | 适用对象 | 题库 | 核心功能 |
|:---|:---|:---|:---|
| **求职者模式** | 有工作经验的求职者 | 求职者题库 (a/b) | 技能测试 → 画像生成 → 成长曲线+成长目标 → 岗位匹配 → 意向岗位管理 |
| **大学生模式** | 应届生 / 在校生 | 大学生题库 (a/b) | 技能测试 → 画像生成 → 成长曲线+成长目标 |
| **HR 模式** | 用人部门 / HR | HR 题库 (c/d) | 自主答题 / AI 代答 → 岗位画像 → JD 生成 → 岗位库管理 → 人才匹配 → 候选人管理 → 面试后评价 |

### 3.2 账号与登录系统

项目采用体验账号机制，无需注册即可使用全部功能。

| 功能点 | 描述 |
|:---|:---|
| **体验账号一键登录** | 登录页提供"体验账号一键登录"按钮，通过 Supabase 匿名认证自动创建会话。所有数据与账号绑定，持久化存储 |
| **本地访客模式** | 无需联网即可使用，数据仅存储在浏览器 localStorage 中，功能完全正常可用。每次使用相同的访客 ID（基于 `crypto.randomUUID()`） |
| **登录界面** | 展示产品特性卡片（技能测评、双向匹配、成长追踪、数据安全），提供体验登录和访客模式两个入口 |
| **退出登录** | 顶部"体验模式"/"本地访客"标签为可点击按钮，展开菜单后可选择"退出登录"返回登录界面（数据保留不丢失） |
| **重置账号数据** | 两个入口：① 顶栏用户菜单 → "重置账号数据"；② 登录页面底部 → "重置体验账号"。确认后清除所有数据（画像历史、成长记录、岗位、候选人、API Key 等），恢复到全新初始状态 |
| **数据隔离** | 画像历史、成长记录等数据按 `userMode`（求职者/大学生/HR）隔离显示，切换模式后仅展示当前模式的数据 |
| **自动登录恢复** | 页面刷新后通过 `isDemoUser` 持久化状态自动恢复登录，无需重新认证。退出登录或重置后可重新进入登录界面 |

### 3.3 首页工作台

每种用户模式均配备风格一致的首页工作台，作为功能导航中心。

#### 求职者工作台

- **"生成技能画像"按钮**：点击后进入岗位选择页，选择目标岗位后开始标准答题流程（17 题）
- **"意向岗位管理"入口**：进入意向岗位管理页，可添加、删除关注的岗位（目标公司、职业类型）
- **"岗位库匹配"入口**：将当前最新技能画像与 HR 发布的岗位进行匹配
- **"成长模式"入口**：进入成长模式，选择历史技能画像后查看成长曲线、目标看板和 AI 成长规划（卡片大小与"生成技能画像"一致，使工作台布局饱满）
- **历史画像管理**：按时间倒序展示所有历史技能画像，每条记录显示测试时间、岗位类型、总评分。支持点击查看画像详情，并提供删除功能（数据按用户模式隔离显示）

#### 大学生工作台

- **"生成技能画像"按钮**：点击后进入岗位选择页，选择目标岗位后开始标准答题流程（17 题）
- **"成长模式"入口**：完成测试后可从结果页或工作台进入，查看成长曲线、目标看板和 AI 成长规划
- **历史画像管理**：按时间倒序展示所有历史技能画像，每条记录显示测试时间、岗位类型、总评分。支持点击查看画像详情，并提供删除功能

#### HR 工作台

- **"生成岗位画像"按钮**：进入 HR 画像生成模式选择页（自主答题 / AI 代答）
- **"岗位库管理"入口**：
  - 展示已发布岗位列表（岗位名称、类型、创建时间、状态）
  - 支持单个岗位的查看、匹配和删除操作
  - 匹配时将岗位画像与求职者数据库进行比对
- **"匹配人才库"入口**：选择已录入的岗位画像，从求职者数据库中匹配合适人选（两阶段匹配）
- **"候选人管理"入口**：
  - 自动同步匹配界面中添加的候选人数据，作为候选人信息的集中管理中心
  - 支持候选人列表查看、按姓名/岗位搜索筛选
  - 核心操作：删除候选人（含确认机制）、发起模拟沟通、标记已面试、信誉评分解锁与评分
  - 卡片上显示候选人数量的实时徽章计数

---

## 四、核心功能模块详细描述

### 模块一：标准化题库测评系统

#### 4.1.1 题库机制

系统内置 **6 套完整题库**（2 个岗位 × 3 种用户模式），每套 17 题：

| 题库标识 | 岗位 | 模式 | 维度数 | 维度体系 |
|:---|:---|:---|:---|:---|
| `ai_engineer_jobseeker` | AI 工程师 | 求职者 | 7 | 基础理论层 / AI 核心技术层 / 工程落地层 / 场景转化层 / 合规治理与软技能 / 前沿研究与创新 / 综合素养 |
| `ai_engineer_student` | AI 工程师 | 大学生 | 7 | 同上 |
| `ai_engineer_hr` | AI 工程师 | HR | 7 | 同上（问题措辞改为"该岗位是否需要…"的需求导向） |
| `modeler_jobseeker` | 3D 建模师 | 求职者 | 8 | 软件操作能力 / 建模技术能力 / UV 与纹理能力 / 美术基础与设计素养 / 渲染与光照能力 / 项目与工程实践能力 / 动画与绑定能力 / 综合素养 |
| `modeler_student` | 3D 建模师 | 大学生 | 8 | 同上 |
| `modeler_hr` | 3D 建模师 | HR | 8 | 同上（问题措辞改为需求导向） |

#### 4.1.2 题目类型与交互机制

| 题目类型 | 说明 |
|:---|:---|
| **单选题** (single) | 每选项对应 1-4 分不等的分值，选择一个答案 |
| **多选题** (multi) | 每选项独立加分，选中负面选项（如"以上均不熟悉"）则该题归零，且与其他选项互斥 |
| **文本题** (text) | 第 17 题为开放文本题（选做），回答将作为 AI 生成画像和匹配的重要参考 |

#### 4.1.3 跳过机制 (SkipRule)

支持两种条件跳过逻辑：
- **正向跳过**：当用户选择特定选项时，自动跳过后续相关题目（如选择"无相关经验"则跳过该维度的深入追问）
- **反向跳过**：当用户未选择指定的必选选项时，自动跳过依赖题（如未勾选任何熟悉领域，则跳过后继详细评估）

跳过规则在题库数据中通过 `skipRule` 配置定义，前端通过 `isQuestionSkippedByRules()` 实时计算当前题是否应跳过。

#### 4.1.4 评分引擎

- **本地计分**（不调用 AI）：题库提交后，由评分引擎本地计算各维度原始分，按维度满分上限归一化
- **星级评定**：≥90 → 5 星，≥70 → 4 星，≥50 → 3 星，≥30 → 2 星，否则 1 星
- **成长曲线定位**：根据综合得分在预设成长曲线上线性插值定位当前阶段节点

---

### 模块二：面向 HR 的 AI 需求精准转化（双模式画像）

HR 可通过以下两种方式生成岗位需求画像：

#### 4.2.1 模式 A：HR 自主答题

| 功能点 | 描述 |
|:---|:---|
| **答题流程** | HR 选择岗位后，根据 HR 端题库逐题作答。题目和选项完全基于题库预设，答题过程中不调用 AI |
| **题库措辞** | 问题均为需求导向，如"该岗位是否需要候选人具备大规模分布式训练经验？" |
| **跳过机制** | 支持与求职者端相同的 skipRule 条件跳过 |
| **结果生成** | 提交后本地评分引擎计算各维度得分，生成岗位画像报告 |

#### 4.2.2 模式 B：AI 代答 + HR 确认

| 功能点 | 描述 |
|:---|:---|
| **四阶段对话引导** | AI 扮演资深 HRBP，分四阶段结构化追问：① 业务背景（产品阶段、团队规模等）→ ② 硬性技能（框架、工具链、工程能力等）→ ③ 软性素质（沟通、学习力、团队协作等）→ ④ 边界条件（薪资、到岗时间、远程偏好等）。每阶段有引导问题 + 文本输入框，支持前进 / 后退导航 |
| **AI 智能代答** | 根据四阶段对话收集的需求信息，AI 自动模拟 HR 完成对应岗位题库的答题，生成完整答案列表 |
| **降级容错** | AI 代答失败时自动降级为本地随机生成答案，确保用户体验不中断 |
| **结果确认** | 代答完成后自动进入与模式 A 相同的评分和画像生成流程，HR 可在结果页面对内容进行确认或微调 |

#### 4.2.3 画像输出与岗位入库

| 功能点 | 描述 |
|:---|:---|
| **综合评分** | 各维度星级 + 百分制综合得分 |
| **文字报告** | AI 参考画像模板自动生成简要文字报告，概括核心优势、待补足项和招聘策略建议 |
| **雷达图可视化** | 前端渲染雷达图，直观展示各维度能力分布 |
| **JD 编辑与微调** | 弹窗中展示 AI 生成的岗位 JD，HR 可在线编辑修改 |
| **一键入库** | 点击"保存 JD 并录入岗位库"将岗位信息存入 Supabase `hr_jobs` 表 |

---

### 模块三：面向求职者的轻量级技能画像构建 + 成长追踪

#### 4.3.1 技能画像生成流程

| 功能点 | 描述 |
|:---|:---|
| **岗位选择** | 求职者 / 大学生先选择目标岗位：AI 工程师 或 3D 建模师 |
| **题库测试** | 加载对应求职者 / 大学生题库，共 17 题。答题过程不调用 AI |
| **去标签化设计** | 全程不收集、不展示学历、学校、年龄等标签信息，仅呈现技能证明数据 |
| **本地计分** | 提交答案后由评分引擎本地计算各维度得分与星级评定 |
| **技能评分柱状图** | 前端渲染横向柱状图，展示各维度得分与满分的对比 |
| **多边形雷达图** | AI 工程师为六边形雷达图（6 维度），3D 建模师为七边形雷达图（7 维度） |
| **AI 生成人物画像** | 调用 DeepSeek API，参考画像模板，根据得分和答案生成专属文字版人物画像报告 |
| **答题结果同步** | 提交后自动将答题记录（题目索引、答案、跳过标记、岗位、模式）存入 Supabase `quiz_results` 表 |
| **画像录入** | 求职者/大学生可点击"保存技能画像"将测评结果存入 Supabase `candidate_profiles` 表。保存时自动生成唯一 `profileId`，同时创建关联的成长记录，并加入 `savedProfileIds` 防止重复保存 |

#### 4.3.2 成长模式（求职者 / 大学生通用）

成长模式入口位于首页工作台"成长模式"卡片，也可从结果页"查看成长分析"按钮进入。

**画像选择机制：**

| 功能点 | 描述 |
|:---|:---|
| **画像选择器** | 进入成长模式后，展示当前模式下所有历史技能画像的卡片列表，每张卡片显示岗位类型、测试日期、总分、星级、维度得分概览 |
| **空状态引导** | 无历史画像时显示引导提示"请先完成技能测试并保存画像" |
| **返回重选** | 在成长详情页可通过左上角返回按钮重新选择画像 |

**成长分析与规划：**

| 功能点 | 描述 |
|:---|:---|
| **成长曲线展示** | 系统展示预设成长曲线（AI 工程师 4 阶段 15 节点 / 3D 建模师 4 阶段 15 节点），横轴为时间（月），纵轴为分数。不同阶段用不同底色区域区分 |
| **当前位置标记** | 根据所选画像的评分结果，在曲线上通过线性插值定位当前所在节点位置，显示高亮圆点标记 |
| **多维度曲线** | 图表中同时绘制各能力维度（基础理论、AI核心、工程落地等）的成长曲线，不同颜色区分，图例可交互 |
| **里程碑展示** | 曲线下方列出关键里程碑节点（如"完成第一个AI API调用项目""完整MLOps管道建立"等），以金色标签展示 |
| **成长目标看板** | 每个维度的当前分值与下一阶段目标分值对比，进度条显示达成率。标红"短板优先"（差距 > 20%）、标黄"需要巩固"（差距 8%-20%）、标绿"已达标" |
| **维度差距分析** | 当前 vs 目标阶段的百分比差距可视化，列出各维度具体分差，红色填充标识差距区域 |
| **AI 成长规划** | 调用 DeepSeek API，根据当前节点、各维度得分和与下一阶段的差距，生成包含技能补足建议、学习里程碑的个人成长方案。支持一键重新生成 |
| **AI 学习资源推荐** | AI 根据技能短板推荐课程、书籍、社区等学习资源（可折叠展开） |
| **AI 项目实践方向** | AI 推荐入门级 / 进阶实践项目，匹配当前能力水平，包含作品集建议和实战技巧（可折叠展开） |
| **历史成绩对比** | 同一用户多次测试后，在底部展示历史成绩列表，当前成绩高亮标记，显示与上次测试的分数变化趋势 |

**成长记录保存机制：**

| 功能点 | 描述 |
|:---|:---|
| **自动保存** | 当 AI 生成成长规划后，自动将成长记录（日期、得分、曲线节点、成长规划内容）更新到对应的技能画像记录中，无需手动操作 |
| **一对一绑定** | 成长记录通过 `profileId` 与技能画像建立一对一关联。同一画像多次进入成长模式时，共享同一份成长记录 |
| **记录去重** | 技能画像保存时同步创建初始成长记录（`growthPlan` 为空），AI 生成规划后自动更新内容，避免重复创建 |

---

### 模块四：意向岗位管理（求职者专属）

| 功能点 | 描述 |
|:---|:---|
| **岗位列表** | 桌面端表格展示 / 移动端卡片布局，显示岗位名称、目标公司、职业类型、创建时间 |
| **添加意向岗位** | 弹窗表单：岗位名称（必填）、目标公司（选填）、职业类型（AI 工程师 / 3D 建模师） |
| **删除岗位** | 带确认弹窗的删除功能 |
| **数据同步** | 通过 `syncJobseekerJob` 存入 Supabase `jobseeker_jobs` 表，支持云端加载和离线操作 |

---

### 模块五：去标签化双向匹配

| 功能点 | 描述 |
|:---|:---|
| **匹配触发** | HR 在岗位库中选择岗位点击"匹配"，或求职者点击"岗位库匹配"按钮 |
| **岗位与人才区分** | AI 工程师和 3D 建模师在匹配时严格区分，仅在同类岗位内匹配 |
| **两阶段匹配引擎** | **第一阶段（向量初筛）**：从人才库快速筛选 Top 候选人；**第二阶段（AI 精排）**：从多个维度精细评分 0-100 分 |
| **求职者前置条件** | 求职者需先完成对应职业的标准化测试才能触发匹配 |
| **信誉权重调整** | 匹配排序时引入信誉星级权重，高信誉求职者获得优先推荐 |
| **匹配结果展示** | 按匹配度从高到低展示候选人卡片：匿名头像、匹配分、信誉分、维度匹配圆环图、匹配亮点标签、待补足项标签、信誉档案区 |
| **双向匿名互动** | 匹配成功后双方在隐藏个人信息的匿名环境中初步沟通 |
| **意愿确认** | 双方均表达意愿后互换企业名称与基本信息，进入正式面试流程 |

---

### 模块六：后置评价与动态信誉机制

| 功能点 | 描述 |
|:---|:---|
| **HR "诚实分"评价** | 面试结束后 HR 对求职者进行 1-5 星"诚实分"打分，填写至少 15 字评价理由。系统提供"技能真实性""沟通坦诚度""经验描述准确性"等预设维度标签（多选） |
| **求职者反向评价** | 求职者可匿名评价面试体验及岗位描述真实性，形成双向约束 |
| **信誉档案展示** | 综合信誉分 + 三个子维度进度条（技能真实性 / 沟通坦诚度 / 经验描述准确性） |
| **信誉回血机制** | 低于 4 星的求职者，在完成新技能测试后，系统在下月 1 日自动恢复至 4 星基准 |
| **评价时效管理** | 匹配算法优先参考近 3 个月评价，历史评价权重随时间衰减 |
| **信誉权重加持** | 高信誉求职者在人才池匹配中获得优先推荐权重 |
| **评价记录** | 评价历史可展开查看，近 3 个月评价高亮标记，生涯评价列表滚动浏览 |
| **数据同步** | 评价数据通过 `syncEvaluation` 存入 Supabase `evaluations` 表 |

---
### 模块七：候选人管理（HR 专属）

作为从匹配界面自动同步的候选人信息集中管理中心，提供完整的候选人状态跟踪与交互功能。

#### 数据同步机制

| 同步场景 | 说明 |
|:---|:---|
| **匹配界面添加** | HR 在匹配结果中点击"添加为候选人"，候选人数据自动写入本地 Store + localStorage，并出现在候选人管理列表中 |
| **去重保护** | 同一候选人不可重复添加，系统自动检测并提示"该候选人已在列表中" |
| **数据持久化** | 候选人数据通过 Zustand persist 中间件持久化到 localStorage，页面刷新或切换模式后数据不丢失 |

#### 核心操作功能

| 功能 | 描述 |
|:---|:---|
| **候选人列表** | Glass-card 风格卡片列表，展示姓名（首字母头像）、岗位类型、匹配度百分比、添加日期，支持 AnimatePresence 动画过渡 |
| **搜索筛选** | 支持按候选人姓名或岗位名称实时搜索过滤，带清除按钮 |
| **删除候选人** | 带确认弹窗（AlertTriangle 图标 + "此操作不可撤销"提示），防止误操作 |
| **发起沟通（模拟聊天）** | 弹窗式聊天界面，HR 可输入文字消息发送，虚拟候选人自动在 0.8-2 秒随机延迟后随机回复预设语句（如"感谢关注""方便周一至周五上午面试"等）。包含消息气泡（HR 蓝紫色靠右 / 候选人灰色靠左）、时间戳、Enter 快捷发送 |
| **状态标记** | "标记已面试"按钮，点击后候选人状态从"确认"切换为"已面试"，按钮变为绿色"已面试"徽章（不可逆） |
| **信誉评分解锁** | 仅"已面试"状态的候选人可查看信誉评分；未面试候选人显示"面试后解锁信誉评分"（锁图标） |
| **信誉评分** | 1-5 星评分弹窗，点击星级（金色填充/空心），提交后计算**多次评分的算术平均值**并乘以 20 转换为 0-100 分制。显示历史评分次数 |

#### 界面交互细节

| 交互要素 | 说明 |
|:---|:---|
| **空状态** | 无候选人时显示引导卡片："暂无候选人 — 前往匹配页面，在匹配结果中添加候选人"，带"前往匹配"按钮 |
| **返回导航** | 页面顶部"返回首页"链接，一键回到 Dashboard |
| **计数徽章** | 页面标题区 + Dashboard 入口卡片均显示实时候选人数量 |
| **视觉风格** | 紫色主题（UserCheck 图标 + purple-500 渐变），与 Dashboard 候选人管理卡片风格统一 |

---
### 模块八：UI 设计规范与交互体验

| 设计要素 | 规范说明 |
|:---|:---|
| **桌面端 / 移动端响应式** | 适配桌面端和移动端浏览器，移动端使用汉堡菜单折叠导航 |
| **模式切换** | 顶部标签栏提供"求职者""大学生""HR"三个 pill 样式标签，切换时重置问答进度 |
| **首页工作台** | 三种用户模式均配备风格一致的首页工作台，含欢迎标语、主操作卡片、快捷入口和底部特性展示 |
| **登录页面** | 居中卡片式布局，展示品牌 Logo + 产品名称 + 描述文案 + 四宫格特性卡片 + 体验登录按钮 + 访客模式按钮 + 重置账号链接。使用 Framer Motion 渐入动画 |
| **用户菜单** | 顶栏"体验模式"/"本地访客"标签为可点击按钮，展开下拉菜单提供"退出登录"和"重置账号数据"两个操作 |
| **AI Key 配置** | 顶部栏显示 API Key 配置状态（已配置绿勾 / 未配置金色脉冲点），点击弹出输入框 + 安全说明 |
| **同步状态指示器** | 全局顶部横幅：蓝色（加载中）+ Loader2 旋转图标 / 金色（同步中）+ Loader2 旋转图标 / 红色（异常）+ AlertTriangle 图标 + 错误详情。支持手动关闭（8 秒后自动恢复显示） |
| **错误边界（ErrorBoundary）** | 全局错误捕获组件，渲染异常时展示友好降级界面："页面加载异常" + 错误详情折叠面板 + "重试"和"刷新页面"按钮，防止白屏崩溃 |
| **页面切换回顶** | 页面路由切换时自动滚动到顶部：模块级禁用浏览器滚动恢复（`history.scrollRestoration = 'manual'`），结合 `useLayoutEffect` 同步滚动 + `useEffect` 异步 RAF/定时器双重保障 |
| **Markdown 报告渲染** | 自实现轻量 Markdown 解析器（无第三方依赖），支持 h1-h4、段落、粗体、列表、分隔线、代码块渲染 |
| **风格统一** | 求职者端、大学生端、HR 端保持一致的视觉风格和交互逻辑 |

---

## 五、环境配置

项目通过 `.env` 文件配置环境变量：

```env
VITE_SUPABASE_URL=https://hbzbnyvjkuqivcfbgumk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

- `VITE_SUPABASE_URL`：Supabase 项目 URL
- `VITE_SUPABASE_ANON_KEY`：Supabase 匿名密钥（可发布密钥，前端安全使用）

---

## 六、路由表

| 路由 | 页面 | 适用模式 | 功能 |
|:---|:---|:---|:---|
| `/` | DashboardPage | 全部 | 首页工作台，模式导航 + 历史记录 + 快捷入口 |
| `/select-position` | PositionSelectPage | 求职者 / 大学生 | 岗位选择（AI 工程师 / 3D 建模师） |
| `/quiz` | QuizPage | 求职者 / 大学生 | 标准化题库答题（17 题） |
| `/quiz/hr-ai` | HRAssistPage | HR | HR 画像生成（自主答题 / AI 代答 + 四阶段对话） |
| `/result` | ResultPage | 全部 | 测评结果报告，支持画像入库 + JD 保存 |
| `/growth` | GrowthPage | 求职者 / 大学生 | 成长曲线 + 差距分析 + 成长目标看板 + AI 规划 |
| `/hr/jobs` | HRJobManagePage | HR | 岗位库管理（查看 / 匹配 / 删除） |
| `/jobs` | JobSeekerJobsPage | 求职者 | 意向岗位管理（添加 / 删除） |
| `/match` | MatchPage | 求职者 / HR | 双向匹配引擎，两阶段匹配 + 匿名沟通 |
| `/hr/candidates` | HRCandidatePage | HR | 候选人管理（列表 / 删除 / 模拟沟通 / 状态标记 / 信誉评分） |
| `/evaluate` | EvaluatePage | HR | 面试后信誉评价（诚实分 + 双向评价） |
| `/seed-data` | SeedDataPage | 全部 | 种子数据管理（查看 / 初始化预置候选人数据） |
| `/supabase-diagnostic` | SupabaseDiagnosticPage | 全部 | Supabase 连接诊断工具 |

---

## 七、数据库表结构

项目使用 Supabase PostgreSQL，共 7 张业务表：

| 表名 | 用途 | 主要字段 |
|:---|:---|:---|
| `candidate_profiles` | 候选人 / 求职者技能画像 | name, position, mode, dimensions(JSON), total_score, star_rating, text_report, curve_node |
| `hr_jobs` | HR 发布的岗位 | name, position, status, description, profile_doc, created_at |
| `jobseeker_jobs` | 求职者关注的意向岗位 | name, position, company, created_at |
| `growth_records` | 成长记录 | date, score, curve_node, growth_plan, profile_id（关联技能画像，一对一关系） |
| `quiz_results` | 答题记录 | question_index, answer, skipped, position, mode |
| `evaluations` | 面试后评价 | honesty_score, dimension_tags, comment |
| `match_results` | 匹配结果 | candidate_id, job_id, match_score, dimension_scores |

---

## 八、运行命令

```bash
npm install       # 安装依赖
npm run dev       # 启动开发服务器 (Vite dev server)
npm run test      # 运行 65 项单元测试 (vitest)
npm run check     # TypeScript 类型检查 (tsc --noEmit)
npm run build     # 生产构建 (vite build)
```

---

## 九、产品核心特色总结

- **纯前端架构**：无需独立后端服务，通过 Supabase SDK 直接实现数据持久化，部署简单
- **零门槛体验**：无需注册，一键登录即可使用全部功能；支持本地访客模式离线使用；提供退出登录和账号数据重置
- **离线优先策略**：Zustand persist + localStorage 双重持久化，用户操作即时响应，远程同步静默降级，刷新/离线数据不丢失
- **本地计分 + AI 辅助**：答题过程中不调用 AI，题库和评分完全基于预设规则；仅在画像生成和成长规划环节调用 DeepSeek API，大幅降低 Token 消耗
- **成长双引擎：曲线 + 目标**：每个职业预设唯一成长曲线（4 阶段 15 节点），每次测试在曲线上标记节点 + 生成多维度量化成长目标
- **从测评到成长的完整闭环**："测试 → 评分 → 画像 → 成长目标 → AI 规划 → 再次测试 → 进步对比"。画像与成长记录一对一绑定，自动同步
- **去标签化公平匹配**：以能力证据替代学历等标签，降低偏见，促进公平就业
- **双模式 HR 画像**：自主答题（精准可控）与 AI 代答（高效便捷）两种模式，AI 代答内置四阶段对话引导确保需求不遗漏
- **候选人全流程管理**：匹配结果一键添加候选人 → 模拟沟通 → 状态跟踪 → 面试标记 → 信誉评分，形成完整招聘闭环
- **动态信誉飞轮**：后置评价体系让真实能力沉淀为长期信誉资产，结合回血机制激励求职者持续成长
- **安全设计**：Supabase RLS 行级安全 + 匿名认证 + 环境变量配置 + API Key 用户自配置
- **错误容错体系**：全局 ErrorBoundary 防止白屏崩溃 + Supabase 超时自动降级 + AI 调用失败回退本地方案 + 未处理 Promise 拒绝全局捕获